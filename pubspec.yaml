name: exambeing
description: A new Flutter project for competitive exams.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Core Packages
  cupertino_icons: ^1.0.8 # Updated
  go_router: ^14.2.0      # Updated
  provider: ^6.1.2      # Updated
  path: ^1.9.0
  path_provider: ^2.1.3
  permission_handler: ^11.3.1 # Updated
  collection: ^1.18.0
  shared_preferences: ^2.2.3
  url_launcher: ^6.3.0 

  # UI & Media Packages
  percent_indicator: ^4.2.3
  pinput: ^5.0.0 # Updated
  google_fonts: ^6.2.1      # Updated
  screenshot: ^3.0.0
  share_plus: ^9.0.0      # Updated

  # To-Do List/Calendar
  table_calendar: ^3.1.2
  intl: ^0.19.0
  
  # Notifications
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.4

  # Rich Text Editor
  flutter_quill: ^9.4.1 # <-- UPDATED to latest stable version

  # Firebase Packages
  firebase_core: ^2.31.0      # <-- UPDATED
  cloud_firestore: ^4.17.3  # <-- UPDATED
  firebase_auth: ^4.19.5      # <-- UPDATED
  google_sign_in: ^6.2.1
  smart_auth: ^2.0.0

  # Local Database
  sqflite: ^2.3.3

  # AdMob Package
  google_mobile_ads: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # Updated
  flutter_launcher_icons: ^0.13.1
  change_app_package_name: ^1.1.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/icon/

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/app_icon.png"

# ❌ (Dependency overrides hata diye gaye, kyonki ab zaroorat nahi hai)
